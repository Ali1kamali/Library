const selectid = (id) => {
    return document.getElementById(id);
};

const selector = (tag) => {

    // if (tag[0] != '.'){
    //     throw new Error(`tag isn't a class`)
    // }
    return document.querySelector(tag);
};

const aa = 3